import { AppConfig } from 'remax/wechat';

const config: AppConfig = {
  pages: ['pages/index/index'],
  window: {
    navigationBarTitleText: 'WildDream Lite',
    navigationBarBackgroundColor: '#282c34'
  }
};

export default config;
